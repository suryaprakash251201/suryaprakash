name: Android Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Android App
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect Flutter app directory
        shell: bash
        run: |
          if [ -f pubspec.yaml ] && [ -d android ]; then
            echo "APP_DIR=." >> "$GITHUB_ENV"
          elif [ -f suryaprakash/pubspec.yaml ] && [ -d suryaprakash/android ]; then
            echo "APP_DIR=suryaprakash" >> "$GITHUB_ENV"
          else
            echo "Flutter app directory not found." >&2
            ls -la
            exit 1
          fi

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: gradle

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Ensure Gradle wrapper is executable
        shell: bash
        run: |
          cd "$APP_DIR"
          chmod +x android/gradlew

      - name: Install dependencies
        shell: bash
        run: |
          cd "$APP_DIR"
          flutter pub get

      - name: Flutter doctor
        shell: bash
        run: |
          cd "$APP_DIR"
          flutter doctor -v || true

      - name: Build APK
        shell: bash
        run: |
          cd "$APP_DIR"
          flutter build apk --release

      - name: Build AppBundle
        shell: bash
        run: |
          cd "$APP_DIR"
          flutter build appbundle --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-apk
          path: |
            build/app/outputs/flutter-apk/app-release.apk
            suryaprakash/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error

      - name: Upload AppBundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-release-bundle
          path: |
            build/app/outputs/bundle/release/app-release.aab
            suryaprakash/build/app/outputs/bundle/release/app-release.aab
          if-no-files-found: error
